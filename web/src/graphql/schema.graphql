type Query {
  user: User
  classes: [Class]
}

type Mutation {
  updateUser(userId: String!, name: String, role: String): User
}

type User {
  id: String!
  name: String
  email: String
  profileName: String
  isTeacher: Boolean
  isNewUser: Boolean
  classes: [Class]
  assignments: [Assignment]
  grades: [Grade]
}

type Class {
  id: String!
  name: String
  image_url: String
  teacherID: String
  subject: String
}

type Assignment {
  id: String!
  name: String
  teacherID: String!
}

type Grade {
  id: String!
  assignmentID: String
  studentID: String
  grade: Int
}
